# Public, non-secret Socket.IO server URL. Use the deployed server origin in production.
# Next.js embeds NEXT_PUBLIC_ values at build time. Never put secrets here.
NEXT_PUBLIC_REALTIME_URL=http://localhost:3001
